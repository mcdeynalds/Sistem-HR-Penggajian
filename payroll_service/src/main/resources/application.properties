# Nama service
spring.application.name=payroll_service

# Port (beda dari employee service!)
server.port=8080

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/db_payroll
spring.datasource.username=postgres
spring.datasource.password=postgres

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Email SMTP
smtp.host=smtp.gmail.com
smtp.port=587
smtp.username=deafriliart@gmail.com
smtp.password=orxa pbvq dizc dpzt

# URL Employee Service
employee.service.url=http://localhost:8080

# Session timeout
server.servlet.session.timeout=60

# Scheduler aktif
spring.task.scheduling.enabled=true

# Disable DevTools restart (untuk menghindari classloader issue)
spring.devtools.restart.enabled=false